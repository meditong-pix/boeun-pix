
<style>
*{box-sizing:border-box;margin:0;padding:0}
.top-bar{display:flex;align-items:center;justify-content:space-between;padding:12px 0 10px;border-bottom:0.5px solid var(--border)}
.badge{font-size:11px;font-weight:500;padding:2px 8px;border-radius:99px;color:var(--text-accent);background:var(--bg-accent)}
.period{font-size:12px;color:var(--text-muted);margin-left:8px}
.tab-bar{display:flex;border-bottom:0.5px solid var(--border)}
.tab{border:none;background:transparent;font-size:13px;font-weight:500;color:var(--text-muted);padding:10px 18px;cursor:pointer;font-family:inherit;border-bottom:2px solid transparent;margin-bottom:-0.5px;transition:all .12s}
.tab.on{color:var(--text-primary);border-bottom-color:var(--text-primary)}
.analysis-header{display:flex;align-items:center;justify-content:space-between;padding:12px 0;border-bottom:0.5px solid var(--border)}
.h-track{width:160px;height:34px;background:var(--surface-1);border:0.5px solid var(--border);border-radius:999px;padding:4px;position:relative;cursor:pointer;user-select:none}
.h-thumb{position:absolute;top:4px;bottom:4px;background:var(--surface-2);border:0.5px solid var(--border-strong);border-radius:999px;display:flex;align-items:center;justify-content:center;gap:3px;cursor:grab;z-index:2;transition:left .2s cubic-bezier(.4,0,.2,1)}
.h-line{width:1.5px;height:11px;background:var(--border-strong);border-radius:1px}
.h-labels{display:flex;position:absolute;inset:0;z-index:1;pointer-events:none}
.h-lbl{flex:1;display:flex;align-items:center;justify-content:center;font-size:12px;color:var(--text-muted);transition:color .15s,font-weight .15s}
.h-lbl.on{color:var(--text-primary);font-weight:500}
.sum-box{padding:14px 0 16px;border-bottom:0.5px solid var(--border)}
.sum-lbl{font-size:15px;font-weight:500;color:var(--text-primary);margin-bottom:7px}
.sum-txt{font-size:13px;color:var(--text-secondary);line-height:1.75;white-space:pre-line}
.sec{margin-top:22px}
.sec-hd{display:flex;align-items:baseline;justify-content:space-between;margin-bottom:8px}
.sec-lbl{font-size:15px;font-weight:500;color:var(--text-primary)}
.sec-ct{font-size:12px;color:var(--text-muted)}
.rows{border-top:0.5px solid var(--border)}
.row{display:flex;align-items:baseline;justify-content:space-between;padding:9px 0;border-bottom:0.5px solid var(--border)}
.row:last-child{border-bottom:none}
.row-l{flex:1;min-width:0}
.row-r{flex-shrink:0;margin-left:16px;text-align:right}
.rtitle{font-size:13px;color:var(--text-primary)}
.rsub{font-size:12px;color:var(--text-secondary);margin-top:2px;line-height:1.5}
.rval{font-size:13px;font-weight:500}
.rval.up{color:var(--text-success)}
.rval.dn{color:var(--text-danger)}
.rmeta{font-size:11.5px;color:var(--text-muted);margin-top:2px}
.pill{display:inline-block;font-size:10.5px;font-weight:500;padding:1px 7px;border-radius:99px;margin-right:5px;vertical-align:middle}
.pill.h{background:var(--bg-danger);color:var(--text-danger)}
.pill.m{background:var(--bg-warning);color:var(--text-warn
